<tr>
	<td><%= link_to test.title, admin_test_path(test) %></td>
	<td><%= test.level %></td>
	<td><%= test.category.title %></td>
	<td><%= test.questions.count %></td>
	<td><%= link_to t('admin.edit'), edit_admin_test_path(test) %></td>
	<td>
		<%= link_to t('admin.delete'),
								admin_test_path(test),
								method: :delete,
								data: { confirm: t('.message') } %> 
	</td>
</tr>
