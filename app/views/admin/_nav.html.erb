<div class="nav nav-admin">
	<span class="nav-admin title"><%= link_to t('.header'), admin_tests_path %></span>

	<div class="nav-admin user">
		<%= t('header.welcome') %>, <b class="nav-admin user-email"><%= current_user.email %></b> |</b>
		<%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
	</div>
</div>