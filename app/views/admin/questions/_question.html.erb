<li>
	<%= link_to question.body, admin_question_path(question) %> |
	<%= link_to t('admin.edit'), edit_admin_question_path(question) %> |
	<%= link_to t('admin..delete'), admin_question_path(question), 
				method: 'delete', data: { confirm: 'Are you sure?' } %>
</li>
